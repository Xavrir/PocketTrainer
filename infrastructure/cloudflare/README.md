# Cloudflare/R2 setup

Proxy `api.pockettrainer.example` to Azure Container Apps. Keep R2 private and publish only signed exercise manifests and course media. Do not route camera frames or landmark streams through Cloudflare.
